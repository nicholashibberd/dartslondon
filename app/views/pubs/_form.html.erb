<%= form_for(@pub, html: {role: 'form'}) do |f| %>
  <% if @pub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pub.errors.count, "error") %> prohibited this pub from being saved:</h2>

      <ul>
      <% @pub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :region %><br>
    <%= f.select :region_id, options_from_collection_for_select(@regions, :id, :name, @pub.region_id) %>
  </div>
  <div class="form-group">
    <%= f.submit 'Submit', class: 'btn btn-default' %>
  </div>
<% end %>
