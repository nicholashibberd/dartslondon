<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% unless homepage? %>
        <a class="navbar-brand" href="#">
          <%= image_tag 'navbar-logo.png' %>
        </a>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="<%= admin_index_regions_path %>" class="dropdown-toggle" data-toggle="dropdown">Regions<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% @regions.each do |region| %>
              <li><%= link_to region.name, edit_region_path(region) %></li>
            <% end %>
            <li><%= link_to 'All Regions', admin_index_regions_path %></li>
          </ul>
        </li>
        <li><%= link_to 'Pubs', admin_index_pubs_path %></a></li>
        <li><%= link_to 'Users', '/users' %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li id="current-user"><%= current_user.email %></li>
        <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
